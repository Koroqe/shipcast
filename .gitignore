.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Per-project artifacts (only commit the _template + _brand seeds, not generated runs)
projects/*/
!projects/_template/
!projects/_brand/

# Project locks
projects/**/.lock
projects/**/logs/

# Local Claude config
.claude/scratchpad.md
.claude/settings.local.json

# Real operator brand packs stay local (only the _brand/ dir + .gitkeep are tracked)
projects/_brand/*/
